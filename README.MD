# SDCARD READER FOR FPGA IMPLEMENTATION
Please install the requirements using:
""" pip install -r requirements.txt """

fix for glib error:
sudo apt-get install --reinstall python-dbus


fix for auto mount:
sudo apt-get install usbmount  
